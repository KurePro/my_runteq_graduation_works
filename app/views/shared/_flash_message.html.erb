<div class="fixed top-24 right-4 z-50 flex flex-col w-fit pointer-events-none">
  <% flash.each do |key, value| %>
    <div class="<%= flash_kind(key) %>">
        <p class="text-lg font-bold leading-none">
          <%= value %>
        </p>
      </div>
    </div>
  <% end %>
</div>
